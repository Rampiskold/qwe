# 🚀 Быстрый старт

## Запуск за 3 шага

### 1. Перейдите в директорию
```bash
cd /srv/sgr-agetn-core-tool-search-context-compression/sgr-agent-core/agent_visualizer
```

### 2. Активируйте виртуальное окружение
```bash
source venv/bin/activate
```

### 3. Запустите Streamlit
```bash
streamlit run app.py
```

## Или используйте скрипт автозапуска

```bash
./run.sh
```

## Что дальше?

1. Откройте браузер по адресу: `http://localhost:8501`
2. Выберите файл с логами из директории `../logs/` или загрузите свой
3. Исследуйте визуализации в трех вкладках:
   - 🗺️ **Роудмап** - последовательность выполнения
   - 📊 **Метрики** - производительность и токены
   - ⏳ **Таймлайн** - хронология событий

## Тестирование без UI

Для анализа логов через Python API:

```bash
python example_usage.py
```

Это создаст HTML визуализации в папке `output/`.

## Примеры команд

### Запуск на другом порту
```bash
streamlit run app.py --server.port 8502
```

### Запуск в headless режиме (для серверов)
```bash
streamlit run app.py --server.headless true
```

### Очистка кэша
```bash
streamlit cache clear
```

## Структура файлов

```
agent_visualizer/
├── app.py              # 🎨 Главное приложение
├── log_parser.py       # 🔍 Парсер логов
├── visualizers.py      # 📊 Графики и диаграммы
├── example_usage.py    # 📝 Примеры использования API
├── run.sh              # 🚀 Скрипт автозапуска
└── README.md           # 📚 Полная документация
```

## Нужна помощь?

Смотрите полную документацию в [README.md](README.md)

