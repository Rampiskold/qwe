# 🤖 SQL Database Agent - Руководство пользователя

## 📋 Описание

SQL Database Agent - это интеллектуальный агент для работы с PostgreSQL базой данных. Он помогает:
- Исследовать структуру базы данных
- Выполнять SQL запросы
- Анализировать данные
- Получать инсайты и рекомендации

## 🚀 Быстрый старт

### 1. Убедитесь что API запущен

```bash
cd /srv/sgr-agetn-core-tool-search-context-compression/postgres-adminer-setup
docker compose ps

# Должен быть запущен sgr-api-standalone на порту 18790
```

### 2. Запустите SQL агента

```python
from sgr_agent_core import create_agent

# Создаем SQL агента
agent = create_agent("sql_database_agent")

# Задаем вопрос
result = await agent.run("Покажи мне список всех таблиц в базе данных")
```

## 💬 Примеры вопросов

### Исследование базы данных

```python
# Узнать какие таблицы есть
"Какие таблицы есть в базе данных?"
"Show me all available tables"

# Понять структуру таблицы
"Какая структура у таблицы dict_currencies?"
"What columns does the app_logs table have?"

# Найти связи между таблицами
"Как связаны таблицы budget_actuals и dict_tribes?"
"Show me the relationships between tables"
```

### Получение данных

```python
# Простые запросы
"Покажи мне все валюты"
"Get all active currencies"

# С фильтрацией
"Покажи логи с уровнем ERROR за последнюю неделю"
"Show me budget records where amount > 100000"

# С сортировкой
"Покажи топ 10 самых больших таблиц по размеру"
"List currencies ordered by name"
```

### Аналитические запросы

```python
# Агрегация
"Сколько записей в каждой таблице?"
"Count ERROR logs by date"
"What's the total budget amount by tribe?"

# Группировка
"Сгруппируй логи по уровню и посчитай количество"
"Group budget actuals by month and show totals"

# JOIN запросы
"Покажи бюджет каждого трайба с названием трайба"
"Show expense items with their categories"
```

### Сложные аналитические задачи

```python
# Временной анализ
"Как менялось количество ERROR логов по дням за последний месяц?"
"Show budget trends over the last quarter"

# Сравнительный анализ
"Какой трайб имеет самый большой бюджет?"
"Compare average amounts across different expense categories"

# Поиск аномалий
"Есть ли в логах необычные паттерны?"
"Find budget records with unusually high amounts"
```

## 🛠️ Возможности агента

### Автоматическое исследование

Агент автоматически:
1. **Изучает структуру БД** - находит нужные таблицы
2. **Проверяет схемы** - понимает типы данных и связи
3. **Строит запросы** - создает оптимальные SQL запросы
4. **Анализирует результаты** - предоставляет инсайты

### Умные запросы

Агент понимает:
- **Естественный язык** - можно спрашивать как у человека
- **Контекст** - помнит предыдущие запросы
- **Намерения** - понимает что вы хотите узнать
- **Ограничения** - автоматически добавляет LIMIT для безопасности

### Безопасность

Агент:
- ✅ Выполняет только SELECT запросы
- ❌ Не может изменять данные (INSERT, UPDATE, DELETE)
- ❌ Не может удалять таблицы (DROP, TRUNCATE)
- ✅ Защищает от SQL-инъекций

## 📊 Примеры использования

### Пример 1: Исследование новой БД

**Вопрос:** "Расскажи мне о базе данных - какие таблицы есть и для чего они нужны?"

**Что делает агент:**
1. Получает список всех таблиц
2. Читает комментарии к таблицам
3. Группирует таблицы по назначению (справочники, данные, логи)
4. Предоставляет структурированный обзор

**Результат:**
```
В базе данных 8 таблиц:

Справочники (dict_*):
- dict_currencies: Справочник валют (7 колонок)
- dict_tribes: Справочник трайбов (6 колонок)
- dict_cfo: Справочник ЦФО (7 колонок)
- dict_functions: Справочник функций SMT (7 колонок)
- dict_expense_items: Справочник статей затрат (10 колонок)

Данные:
- budget_actuals: Фактические данные по бюджету (18 колонок)
- capex_it_budget: Бюджеты CAPEX IT (34 колонки)

Служебные:
- app_logs: Логи приложения (8 колонок)
```

### Пример 2: Аналитический запрос

**Вопрос:** "Какой трайб имеет самый большой суммарный бюджет?"

**Что делает агент:**
1. Находит таблицы budget_actuals и dict_tribes
2. Изучает их схемы для понимания связей
3. Строит JOIN запрос с агрегацией
4. Выполняет запрос
5. Анализирует топ результаты

**SQL запрос (создан агентом):**
```sql
SELECT 
    dt.tribe_name,
    COUNT(ba.id) as record_count,
    SUM(ba.amount) as total_budget,
    AVG(ba.amount) as avg_amount
FROM dict_tribes dt
LEFT JOIN budget_actuals ba ON dt.id = ba.tribe_id
GROUP BY dt.tribe_name
ORDER BY total_budget DESC
LIMIT 10;
```

**Результат:**
```
Топ трайбов по бюджету:

1. Platform: 45.2M ₽ (245 записей, средний чек 184K ₽)
2. Data: 38.7M ₽ (189 записей, средний чек 205K ₽)
3. Mobile: 31.5M ₽ (156 записей, средний чек 202K ₽)

Инсайты:
- Platform лидирует по общему бюджету
- Data имеет самый высокий средний чек
- Топ-3 трайба составляют 60% от общего бюджета
```

### Пример 3: Временной анализ

**Вопрос:** "Как менялось количество ERROR логов по дням за последнюю неделю?"

**Что делает агент:**
1. Находит таблицу app_logs
2. Проверяет наличие колонок created_at и log_level
3. Строит запрос с группировкой по дням
4. Анализирует тренд

**SQL запрос:**
```sql
SELECT 
    DATE(created_at) as date,
    COUNT(*) as error_count
FROM app_logs
WHERE log_level = 'ERROR'
    AND created_at > NOW() - INTERVAL '7 days'
GROUP BY DATE(created_at)
ORDER BY date DESC;
```

**Результат:**
```
ERROR логи за последнюю неделю:

2024-12-17: 23 ошибки
2024-12-16: 18 ошибок
2024-12-15: 31 ошибка ⚠️
2024-12-14: 15 ошибок
2024-12-13: 19 ошибок

Анализ:
- Средний уровень: ~21 ошибка/день
- Пик 15 декабря (31 ошибка) - стоит исследовать
- Общий тренд стабильный
```

## 🎯 Best Practices

### 1. Задавайте конкретные вопросы

```python
# ❌ Плохо
"Покажи данные"

# ✅ Хорошо
"Покажи топ 10 трайбов по суммарному бюджету за 2024 год"
```

### 2. Используйте контекст

```python
# Агент помнит предыдущие запросы
"Покажи таблицу budget_actuals"
# Затем можно уточнить:
"А теперь покажи только записи где amount > 1000000"
```

### 3. Просите объяснения

```python
"Объясни структуру таблицы capex_it_budget"
"Как связаны таблицы в этой базе данных?"
"Что означают колонки в таблице app_logs?"
```

### 4. Запрашивайте инсайты

```python
"Проанализируй распределение бюджета по трайбам"
"Найди аномалии в логах за последний месяц"
"Какие тренды видны в данных?"
```

## ⚙️ Конфигурация

### Настройки агента (agents.yaml)

```yaml
sql_database_agent:
  llm:
    model: "qwen3-30b-a3b-instruct-2507"
    temperature: 0.2  # Низкая для точности
    max_tokens: 8000
  
  execution:
    max_steps: 15      # Достаточно для сложных запросов
    max_iterations: 20 # Для итеративного анализа
```

### API Endpoint

По умолчанию: `http://localhost:18790`

Для изменения отредактируйте файлы инструментов в:
`sgr_agent_core/tools/sql_agent/`

## 🔧 Troubleshooting

### Агент не может подключиться к БД

```bash
# Проверьте что API запущен
curl http://localhost:18790/health

# Если не работает, перезапустите
cd /srv/sgr-agetn-core-tool-search-context-compression/postgres-adminer-setup
docker compose restart api
```

### Агент возвращает ошибку SQL

Агент автоматически:
- Проверит синтаксис
- Предложит исправления
- Попробует альтернативный запрос

Вы можете:
- Уточнить вопрос
- Попросить показать SQL запрос
- Попросить объяснить ошибку

### Агент возвращает слишком много данных

Агент автоматически использует LIMIT, но вы можете:
```python
"Покажи только первые 10 записей"
"Ограничь результат 50 строками"
```

## 📚 Дополнительные ресурсы

- **API документация:** `/srv/.../postgres-adminer-setup/API_README.md`
- **Примеры SQL:** `/srv/.../postgres-adminer-setup/SQL_QUERY_EXAMPLES.md`
- **Инструменты:** `/srv/.../sgr_agent_core/tools/sql_agent/README.md`
- **Swagger UI:** `http://localhost:18790/docs`

## 🎓 Продвинутое использование

### Цепочка запросов

```python
# 1. Исследование
"Какие таблицы связаны с бюджетом?"

# 2. Детали
"Покажи схему таблицы budget_actuals"

# 3. Анализ
"Посчитай средний бюджет по каждому трайбу"

# 4. Углубление
"Покажи детали для трайба с самым большим бюджетом"
```

### Экспорт результатов

```python
# Попросите агента сохранить результаты
"Сохрани результаты анализа бюджета в отчет"

# Или получите SQL запрос для использования в других инструментах
"Покажи SQL запрос который ты использовал"
```

### Создание дашбордов

```python
# Попросите агента предложить метрики
"Какие ключевые метрики можно отслеживать в этой БД?"
"Предложи набор запросов для дашборда по бюджету"
```

---

**Примечание:** Агент постоянно обучается и улучшается. Если вы нашли проблему или у вас есть предложения, сообщите разработчикам!
